# VideoAnalysis.Contracts

Purpose:

- frontend-backend communication surface
- commands, queries, DTOs, public application interfaces

Must not contain:

- persistence code
- FFmpeg code
- domain implementation details
